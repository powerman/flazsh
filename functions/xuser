read XUSER TTY_OR_DISPLAY DISPLAY _ <<<"$(w -f | grep ':[0-9][. ]' | head -n 1)"
[[ $TTY_OR_DISPLAY == :* ]] && DISPLAY="$TTY_OR_DISPLAY"
unset TTY_OR_DISPLAY
export XUSER DISPLAY
export XHOME="$(grep "^${XUSER}:" /etc/passwd | cut -d: -f 6)"
[[ -n "$XUSER" && -n "$DISPLAY" && -d "$XHOME" ]]
